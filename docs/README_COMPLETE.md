# 医疗知识图谱术语与规则治理工具

[![CI/CD](https://github.com/your-org/medical-governance/actions/workflows/ci-cd.yml/badge.svg)](https://github.com/your-org/medical-governance/actions)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

基于 **LLM+KG 双引擎协同** 的医疗数据治理平台，解决术语标准化、规则自动化和拒付解释三大痛点。

## ✨ 核心特性

### 🔄 智能术语治理
- **双阶段映射**: 向量召回 + LLM推理
- **人工审核**: 完整的反馈闭环
- **增量学习**: Few-shot示例库自动更新

### ⚙️ 规则自动化
- **Text-to-SHACL**: 自然语言 → 可执行规则
- **插件化架构**: 支持多种规则类型扩展
- **沙箱测试**: 历史数据回测验证

### 🔍 GraphRAG 解释
- **证据链**: 完整的拒付路径追溯
- **自然语言**: LLM生成通俗解释
- **政策问答**: 智能助手

### 🔐 生产就绪
- **JWT认证**: OAuth2标准
- **审计日志**: 完整操作记录
- **异步队列**: 重任务后台处理
- **CI/CD**: 自动化部署流水线

---

## 🚀 快速开始

### 本地开发

**后端**:
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

**前端**:
```bash
cd frontend
npm install
npm run dev
```

访问 http://localhost:3000

### Docker部署

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f
```

---

## 📖 文档

- [启动指南](STARTUP_GUIDE.md) - 本地开发环境配置
- [生产部署](PRODUCTION_DEPLOYMENT.md) - 生产环境部署指南
- [API文档](http://localhost:8000/docs) - 启动后访问

---

## 🏗️ 技术架构

```
┌─────────────────────────────────────────┐
│          Vue 3 前端界面                  │
│  (政策上传 | 规则审核 | 术语工作台)      │
└────────────────┬────────────────────────┘
                 │ REST API
┌────────────────┴────────────────────────┐
│         FastAPI 后端服务                 │
│  ┌──────────┐  ┌──────────┐  ┌────────┐│
│  │ Ingest   │  │ Rules    │  │GraphRAG││
│  │ Service  │  │ Compiler │  │Service ││
│  └──────────┘  └──────────┘  └────────┘│
└────────────────┬────────────────────────┘
                 │
    ┌────────────┼────────────┐
    │            │            │
┌───▼───┐   ┌───▼───┐   ┌───▼────┐
│Neo4j  │   │Milvus │   │Postgres│
│(图谱) │   │(向量) │   │(关系)  │
└───────┘   └───────┘   └────────┘
```

---

## 🔑 默认账号

- **管理员**: admin / admin123
- **审核员**: reviewer / reviewer123

⚠️ **生产环境请立即修改密码**

---

## 📊 API端点

### 认证
- `POST /api/v1/auth/login` - 登录获取Token
- `GET /api/v1/auth/me` - 获取当前用户信息

### 数据接入
- `POST /api/v1/ingest/document` - 上传政策文档

### 术语治理
- `POST /api/v1/terminology/normalize` - 术语标准化
- `POST /api/v1/terminology/feedback` - 提交审核反馈

### 规则管理
- `POST /api/v1/rules/compile` - 编译规则
- `POST /api/v1/rules/test` - 沙箱测试

### 解释服务
- `GET /api/v1/explanation/rejection/{patient_id}` - 拒付解释
- `POST /api/v1/explanation/query` - 政策问答

---

## 🛠️ 开发指南

### 添加新的规则解析器

1. 继承 `RuleParserPlugin`:
```python
class MyCustomParser(RuleParserPlugin):
    def can_handle(self, policy_type: str) -> bool:
        return "my_type" in policy_type
    
    async def parse(self, text: str, llm) -> SHACLModel:
        # 实现解析逻辑
        pass
```

2. 在 `RuleCompiler` 中注册:
```python
self.plugins.append(MyCustomParser())
```

### 切换LLM提供商

实现 `LLMProvider` 接口:
```python
class OpenAIProvider(LLMProvider):
    async def generate(self, prompt: str, schema: dict = None) -> str:
        # 调用 OpenAI API
        pass
```

---

## 📝 许可证

MIT License - 详见 [LICENSE](LICENSE)

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

## 📧 联系方式

- 项目主页: https://github.com/your-org/medical-governance
- 问题反馈: https://github.com/your-org/medical-governance/issues
